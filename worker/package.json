{
  "name": "bookclub-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API for BookClub",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
